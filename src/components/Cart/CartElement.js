import React, { Component } from 'react';
import './style.sass';

export default class CartElement extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                Cart element
            </div>
        )
    }
}